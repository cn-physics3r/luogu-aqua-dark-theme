# AGENTS.md

## 项目概览

这是一个通过 Stylus 加载的洛谷暗色 UserStyle。项目故意保持简单：没有 JavaScript、构建工具、运行时依赖或外部资源，最终交付物就是可以复制到 Stylus 的单个 CSS 文件。

## 文件结构

- `luogu-codex-dark.user.css`：唯一的样式入口和可分发文件。
- `README.md`：安装方式、覆盖范围、设计约束和手动验证清单。
- `AGENTS.md`：本项目的协作和维护约定。

不要为了拆分 CSS 而引入构建系统。当前文件规模适合通过清晰的区块和注释保持模块化；只有在样式规模显著增长时，才重新评估源码拆分和打包。

## CSS 组织顺序

`luogu-codex-dark.user.css` 应按以下依赖方向组织：

```text
主题令牌
  ↓
基础样式
  ↓
导航和页面外壳
  ↓
页面专属样式
  ↓
跨页面组件和语义状态
  ↓
响应式覆盖
```

新增规则应遵循以下原则：

1. 颜色、阴影、圆角和状态色优先使用 `:root` 中的 `--lc-*` 变量。
2. 只在确认跨页面复用时增加通用规则；页面特例放入对应的首页、题目列表或题目页区块。
3. 原生 `input`、`button`、`table`、`pre` 等选择器必须限制在已知的洛谷应用根节点内，避免无意覆盖站点外层结构。
4. 选择器优先依赖稳定的页面根节点和业务类名，避免不必要的多层 `>` DOM 路径。
5. 新选择器必须先在浏览器中确认实际 DOM；不要为尚未验证的题解、评测记录或个人主页组件猜写类名。
6. 保持页面信息架构、主要布局、赛事图片、头像和轮播图不变。不要增加图片滤镜、JavaScript 或外部资源。
7. `!important` 只用于确实需要压过洛谷原样式的视觉属性；如果可以通过更清晰的作用域解决，优先减少选择器强度。

## 设计令牌

主题颜色集中在 `luogu-codex-dark.user.css` 的 `:root` 区块。修改主题时先调整令牌，不要在页面规则中复制新的硬编码颜色。变量按视觉角色命名，例如画布、表面、边框、文字、强调色、控件、代码块和语义状态。

## 文档和版本

- CSS 的 `@version` 使用三段式版本号。仅整理代码且不改变视觉行为时使用补丁版本；改变页面视觉行为时至少更新补丁版本并同步 README。
- README 中的覆盖范围、设计约束和验证日期必须与 CSS 实际行为一致。
- 如果修改题目页背景、图片处理或页面布局，必须同时检查 README 的设计约束。

## 验证

每次修改后至少执行：

```powershell
git diff --check
git status --short --branch
```

然后在 Stylus 中手动检查：

- 首页登录态：顶栏、左右侧栏、轮播图、赛事面板、评论和 Highcharts 图表。
- `/problem/list`：筛选器、搜索框、分页、题目卡片和进度条。
- `/problem/P1000`：题目卡片、题面、代码块、标签、按钮和分隔线。
- 移动端宽度：页面宽度、卡片圆角和代码块横向滚动。
- 图片资源：赛事图片、头像和轮播图不应被暗色滤镜处理。

CSS 的主要风险是选择器失效或作用域过宽，因此浏览器手动验证比引入复杂测试框架更重要。

## Git 约定

保留用户已有改动，不使用破坏性重置命令。除非用户明确要求，不创建提交、不推送远程，也不修改与主题维护无关的文件。
