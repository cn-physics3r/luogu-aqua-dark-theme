# AGENTS.md

## 项目概览

这是一个通过 Stylus 加载的洛谷暗色 UserStyle。项目保持单文件交付：没有 JavaScript、构建工具、运行时依赖或外部资源，最终文件可以直接复制到 Stylus。

## 文件结构

- `luogu-aqua-dark.user.css`：唯一的主代码、样式入口和可分发文件。它由原 `test.css` 改名而来，后续工作以此文件为准。
- `README.md`：面向使用者的安装方式、覆盖范围、配色、设计边界和本地更新说明。
- `AGENTS.md`：项目结构、选择器维护、版本、验证和 Git 协作约定。
- `scripts/check-userstyle.ps1`：零依赖静态检查脚本，只用于维护，不参与 UserStyle 分发。

不要重新维护第二份 CSS 入口，也不要为了拆分当前规模的 CSS 引入构建系统；通过清晰的区块和注释保持模块化。

## CSS 组织顺序

`luogu-aqua-dark.user.css` 应按以下依赖方向组织：

```text
UserStyle 元数据和作用域
  ↓
主题令牌与洛谷颜色令牌映射
  ↓
页面基础样式、链接和滚动条
  ↓
导航和页面外壳
  ↓
卡片、面板、菜单和弹窗
  ↓
表单、按钮、表格、代码块和旧版评论
  ↓
语义状态与经验证的页面特例
  ↓
首页、Highcharts 和题单广场
```

新增规则应遵循以下原则：

1. 颜色、阴影、圆角和状态色优先使用 `:root` 中的 `--lct-*` 变量；洛谷的 `--lfe-color--*` 和 `--lcolor--*` 令牌通过同一区块映射。
2. 只在确认跨页面复用时增加通用规则；页面特例放入对应的页面或组件区块。
3. 原生 `input`、`button`、`table`、`pre` 等选择器必须限制在已知的洛谷应用根节点内，避免无意覆盖站点外层结构。
4. 选择器优先依赖稳定的页面根节点和业务类名，避免不必要的多层 `>` DOM 路径。
5. 新选择器必须先在浏览器中确认实际 DOM；不要为尚未验证的题解、评测记录或个人主页组件猜写类名。
6. 涉及页面样式修复时，必须先连接可调试的真实浏览器，读取目标页面 DOM、元素 class/id 和计算样式，确认实际元素名后才能新增或修改选择器；浏览器未连接、元素未确认或只能依据截图推测时，必须暂停 CSS 修复并报告阻塞原因。
7. 保持页面信息架构、主要布局、赛事图片、头像和轮播图不变；不要增加图片滤镜、JavaScript 或外部资源。
8. `!important` 只用于确实需要压过洛谷原样式的视觉属性；如果可以通过更清晰的作用域解决，优先减少选择器强度。

当前实现优先通过主题令牌和跨页面组件规则提供基础覆盖；页面特例只用于已经在真实浏览器中确认的结构。不要预先猜写站点尚未出现的类名。

## 易失选择器回归表

以下规则依赖洛谷当前 DOM、内联样式或前端编译产物。它们不是待删除代码，但站点更新后应优先在真实浏览器中复查：

| 页面/组件 | 关键选择器 | 风险来源 | 回归重点 |
| --- | --- | --- | --- |
| 通用下拉列表 | `.dropdown:has(> ul > li)` | 弹层可能在 `#app` 内或追加到 `body` | 表面、选项悬停和难度徽章 |
| 题目、比赛与热力格 | `[style*="background-color: rgb(...)"]` | 依赖站点内联颜色字面值 | 类别颜色、题数色阶、文字对比度和边框 |
| 评测状态 | `.status-link:has(.status-name[style*="background: rgb(...)"])` | 依赖状态节点和内联颜色 | Accepted/Unaccepted 与分数层级 |
| IDE CodeMirror | `.cm-content :is(.ͼ*, .tok-*, .cm-*)` | `ͼ*` 是 CodeMirror 生成类 | 关键字、字符串、注释、行号和选区 |
| 页面识别 | `:has(> .filter-card)`、`:has(.row-wrap > .row .detail)` | 以稳定子树识别页面 | 防止跨页面误命中 |

不要仅凭静态代码删除或泛化这些选择器。修改前应读取实际 DOM、匹配规则、内联样式和计算样式；修改后重新加载完整 UserStyle 并复查命中数量和视觉结果。

IDE 模式当前使用 CodeMirror 6。站点更新编辑器后，应重新检查 `.cm-editor`、`.cm-gutters`、`.cm-lineNumbers` 和实际 token 类名。

## 设计令牌

主题颜色集中在 `luogu-aqua-dark.user.css` 的 `:root` 区块。修改主题时先调整 `--lct-*` 令牌，不要在页面规则中复制新的硬编码颜色。变量按视觉角色命名，例如画布、表面、边框、文字、强调色、控件、代码块和语义状态。

用于识别洛谷内联样式的 `rgb(...)` 属性选择器必须保留站点字面值；它们只用于匹配 DOM，不是主题颜色声明。改变这类选择器前必须在真实页面核对当前内联值。

## 文档和版本

- CSS 的 `@version` 使用三段式版本号；当前版本以 CSS 元数据为唯一来源，并由静态检查脚本核对 README。
- `@namespace` 使用当前仓库标识 `github.com/cn-physics3r/luogu-aqua-dark-theme`；修改它会改变 Stylus 样式身份，后续不得仅为文字整理再次迁移。
- 仅整理代码且不改变视觉行为时使用补丁版本；改变页面视觉行为时至少更新补丁版本并同步 README。
- README 只保留面向使用者的安装、覆盖范围、配色、设计边界和本地更新说明；维护流程、选择器细节、验证清单和 Git 约定只写入 AGENTS。
- README 中的覆盖范围必须与 CSS 实际行为一致，不得继续描述已被替换的旧版专属规则。
- 如果修改图片处理、页面布局或语义状态，必须同时检查 README 的设计边界和覆盖说明。

## 验证

每次修改后至少执行：

```powershell
.\scripts\check-userstyle.ps1
git diff --check
git status --short --branch
```

静态脚本检查唯一 CSS 入口、元数据、CSS/README 版本一致性、主站作用域、外部资源、花括号、主题令牌引用，以及 `--lfe-color--*` 十六进制映射与 `--lcolor--*` RGB 映射的一致性。CSS 无构建步骤。

涉及页面样式时，先在真实页面检查 DOM、匹配规则、内联样式、计算颜色、边框、伪元素和 SVG；再修改 CSS。修改后将完整 `luogu-aqua-dark.user.css` 重新粘贴到 Stylus、保存并刷新页面，然后至少检查：

- 首页登录态：顶栏、现代导航、左右侧栏、应用菜单、卡片、评论、轮播图和 Highcharts 图表。
- 题单广场：分类卡片的普通/选中状态、题单卡片、系列导航、进度圆环和页面横向布局。
- 题目列表与题目页：筛选器、搜索框、按钮、卡片、表格、题面、代码块、标签和分隔线。
- 题目提交页 IDE 模式：语言选择器、下拉菜单、O2 选项、编辑器背景、代码 token、行号栏、活动行、选区、输入/输出工具栏和提交按钮。
- 评测记录页：筛选控件、记录表格、状态标签、详情文字和分页按钮。
- 个人主页：资料导航、统计字段、资料卡片、热力格与右侧图例的颜色对应关系。
- 移动端宽度：基础卡片、表单、代码块和页面横向溢出情况。
- 图片资源：赛事图片、头像、轮播图及视频不应被暗色滤镜处理。

CSS 的主要风险是选择器失效或作用域过宽，因此真实浏览器和 Stylus 中的手动验证比引入复杂测试框架更重要。若最终 UserStyle 未在 Stylus 中保存并刷新，必须明确标记浏览器验证仍待完成。

## Git 约定

保留用户已有改动，不使用破坏性重置命令。只提交本次请求涉及的 CSS 入口和文档；除非用户明确要求，不推送远程。用户明确要求提交时，提交前必须核对 `git diff` 和暂存区范围。
