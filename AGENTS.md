# AGENTS.md

## 项目概览

这是一个通过 Stylus 加载的洛谷暗色 UserStyle。项目保持单文件交付：没有 JavaScript、构建工具、运行时依赖或外部资源，最终文件可以直接复制到 Stylus。

## 文件结构

- `luogu-aqua-dark.user.css`：唯一的主代码、样式入口和可分发文件。它由原 `test.css` 改名而来，后续工作以此文件为准。
- `README.md`：安装方式、当前覆盖、设计约束和手动验证清单。
- `AGENTS.md`：本项目的协作和维护约定。

不要重新维护第二份 CSS 入口，也不要为了拆分当前规模的 CSS 引入构建系统；通过清晰的区块和注释保持模块化。

## CSS 组织顺序

`luogu-aqua-dark.user.css` 应按以下依赖方向组织：

```text
UserStyle 元数据和作用域
  ↓
主题令牌与洛谷颜色令牌映射
  ↓
页面基础样式、链接和滚动条
  ↓
导航和页面外壳
  ↓
卡片、面板、菜单和弹窗
  ↓
表单、按钮、表格、代码块和旧版评论
  ↓
语义状态、标签和 Highcharts
```

新增规则应遵循以下原则：

1. 颜色、阴影、圆角和状态色优先使用 `:root` 中的 `--lct-*` 变量；洛谷的 `--lfe-color--*` 和 `--lcolor--*` 令牌通过同一区块映射。
2. 只在确认跨页面复用时增加通用规则；页面特例放入对应的页面或组件区块。
3. 原生 `input`、`button`、`table`、`pre` 等选择器必须限制在已知的洛谷应用根节点内，避免无意覆盖站点外层结构。
4. 选择器优先依赖稳定的页面根节点和业务类名，避免不必要的多层 `>` DOM 路径。
5. 新选择器必须先在浏览器中确认实际 DOM；不要为尚未验证的题解、评测记录或个人主页组件猜写类名。
6. 涉及页面样式修复时，必须先连接可调试的真实浏览器，读取目标页面 DOM、元素 class/id 和计算样式，确认实际元素名后才能新增或修改选择器；浏览器未连接、元素未确认或只能依据截图推测时，必须暂停 CSS 修复并报告阻塞原因。
7. 保持页面信息架构、主要布局、赛事图片、头像和轮播图不变；不要增加图片滤镜、JavaScript 或外部资源。
8. `!important` 只用于确实需要压过洛谷原样式的视觉属性；如果可以通过更清晰的作用域解决，优先减少选择器强度。

## 设计令牌

主题颜色集中在 `luogu-aqua-dark.user.css` 的 `:root` 区块。修改主题时先调整 `--lct-*` 令牌，不要在页面规则中复制新的硬编码颜色。变量按视觉角色命名，例如画布、表面、边框、文字、强调色、控件、代码块和语义状态。

## 文档和版本

- CSS 的 `@version` 使用三段式版本号；当前主代码版本为 `0.1.12`。
- 仅整理代码且不改变视觉行为时使用补丁版本；改变页面视觉行为时至少更新补丁版本并同步 README。
- README 中的覆盖范围、设计约束和验证清单必须与 CSS 实际行为一致，不得继续描述已被替换的旧版专属规则。
- 如果修改图片处理、页面布局或语义状态，必须同时检查 README 的设计约束和覆盖说明。

## 验证

每次修改后至少执行：

```powershell
git diff --check
git status --short --branch
```

CSS 无构建步骤；同时应进行静态检查，例如确认 UserStyle 元数据、`www.luogu.com.cn` 作用域、花括号配对以及主题令牌定义没有明显错误。然后在 Stylus 中手动检查：

- 首页登录态：顶栏、现代导航、左右侧栏、应用菜单、卡片、评论、轮播图和 Highcharts 图表。
- 题目列表与题目页：筛选器、搜索框、按钮、卡片、表格、题面、代码块、标签和分隔线。
- 评测记录页：筛选控件、记录表格、状态标签、详情文字和分页按钮。
- 移动端宽度：基础卡片、表单、代码块和页面横向溢出情况。
- 图片资源：赛事图片、头像、轮播图及视频不应被暗色滤镜处理。

CSS 的主要风险是选择器失效或作用域过宽，因此真实浏览器和 Stylus 中的手动验证比引入复杂测试框架更重要。

## Git 约定

保留用户已有改动，不使用破坏性重置命令。只提交本次请求涉及的 CSS 入口和文档；除非用户明确要求，不推送远程。用户明确要求提交时，提交前必须核对 `git diff` 和暂存区范围。
